# 口播智剪使用说明

## 普通用户

1. 安装 Windows x64 版本。
2. 打开设置，配置大模型 API；可添加多个候补 API。
3. 在「语音识别」选择在线或本地模式。
4. 选择一次输出目录，软件会永久记住。
5. 按顺序拖入视频，启动全自动队列。
6. 软件按视频逐条执行：语音识别 → AI 重组 → 导出。
7. AI 失败会暂停队列；修复 API 后点击继续。已完成的识别断点不会重复消耗资源。

## 语音识别

- 在线识别：速度和时间戳精度通常更好，需要用户自己的 Whisper API。
- 本地识别：无需 API，首次会自动下载 Whisper 模型，之后永久缓存在本机。
- 完整步骤：[语音识别教程](docs/语音识别教程.md)

## 提高成片质量

- 原视频要包含强钩子、明确痛点、核心卖点、证据或信任背书、行动句。
- 开启「仅保留通顺度最高 3 条」。
- 目标时长不要明显超过原素材可用内容，否则只能硬凑或无法生成。
- 识别完成后排除口头禅、重复句和无关内容。
- 大模型建议使用指令遵循和中文能力较强的模型。

软件只能从原口播中选句和重排。原视频没有价格、证据、优惠或行动句时，AI 无法凭空剪出这些信息。

## 批量队列

- 视频按拖入顺序串行处理。
- 每完成一条会清理临时文件和内存，再处理下一条。
- 普通识别或导出失败会记录错误并继续下一条。
- 大模型全部失败时暂停，避免输出本地乱序兜底成片。
- 可重置单条任务，从语音识别重新开始。

## 导出

- 默认 1080P，保持原视频比例，不拉伸。
- 字幕可开关。
- 输出目录只需选择一次，后续可按需修改。
- 导出时间取决于视频时长、变体数量、字幕、编码器和电脑性能。

## 开发者

```powershell
git clone https://github.com/cglyvip/kbcut.git
cd kbcut
npm ci
npm run dev
```

完整检查：

```powershell
npm run check
```

Windows 安装包：

```powershell
npm run dist
```
