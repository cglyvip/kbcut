# 口播智剪 - 使用说明

## 项目简介

这是一个**口播视频AI自动剪辑工具**，专门为千川投流口播视频设计，能够智能分析视频内容并自动生成多个视频变体版本。

### 核心功能

1. **视频导入** - 支持多种视频格式（mp4、mov、avi、mkv、flv、wmv）
2. **语音识别** - 提供在线和本地两种ASR方案
3. **AI内容分析** - 自动识别口播句子的角色类型（钩子、卖点、展示、逼单、废话）
4. **变体生成** - 根据规则自动生成多个视频版本
5. **视频导出** - 批量导出剪辑后的视频文件

---

## 环境要求

### 必需软件

1. **Node.js** - v22.16.0 或更高版本
2. **FFmpeg** - 用于视频处理和音频提取

### 当前环境检查结果

✅ **Node.js**: v22.16.0（已安装）
✅ **npm**: 10.9.4（已安装）
✅ **FFmpeg**: 6.1.1（已安装）
✅ **项目依赖**: 已安装（node_modules 存在）

---

## 项目结构

```
cut-claude/
├── src/
│   ├── main/           # Electron主进程
│   │   ├── services/   # 核心服务
│   │   │   ├── asr-online.ts    # 在线语音识别（Whisper API）
│   │   │   ├── asr-local.ts     # 本地语音识别
│   │   │   ├── ai-analysis.ts   # AI内容分析
│   │   │   ├── variant-generator.ts  # 变体生成器
│   │   │   └── video-export.ts  # 视频导出
│   │   └── utils/
│   │       ├── ffmpeg-path.ts   # FFmpeg路径查找
│   │       └── audio.ts         # 音频处理
│   ├── preload/        # Electron预加载脚本
│   └── renderer/       # React前端界面
│       └── src/
│           ├── components/   # UI组件
│           │   ├── VideoImport/    # 视频导入区域
│           │   ├── AsrPanel/       # 语音识别面板
│           │   ├── ExportPanel/    # 导出控制面板
│           │   └── AnalysisPanel/  # 分析结果面板
│           └── stores/        # Zustand状态管理
├── out/                # 编译输出目录
├── package.json        # 项目配置
├── electron.vite.config.ts  # Electron-Vite配置
└── electron-builder.yml     # 打包配置
```

---

## 使用步骤

### 1. 启动软件

打开终端（CMD或PowerShell），进入项目目录并运行：

```bash
cd "d:\Program Files\Green\cut-claude\cut-claude"
npm run dev
```

软件窗口会自动弹出，显示主界面。

---

### 2. 导入视频（第1步）

**操作方式**：
- 点击界面上的"导入视频"区域
- 在弹出的文件选择对话框中选择口播视频文件
- 软件会自动解析视频信息（时长、分辨率、帧率、编码等）

**支持格式**：
- MP4、MOV、AVI、MKV、FLV、WMV

**自动提取信息**：
- 文件名、时长、分辨率、帧率、文件大小、编码格式

---

### 3. 语音识别（第2步）

软件提供**两种ASR模式**：

#### 方式A: 在线模式（推荐）

**优势**：识别准确率高，支持词级时间戳

**配置要求**：
- API Key（Whisper API密钥）
- API地址（如 `https://api.openai.com` 或其他兼容服务）
- 模型名称（默认 `whisper-1`）

**配置步骤**：
1. 在ASR面板选择"在线模式"
2. 输入API Key
3. 输入API地址
4. 选择模型（可选）
5. 点击"开始识别"

#### 方式B: 本地模式

**优势**：无需API配置，隐私性强

**使用步骤**：
1. 选择"本地模式"
2. 点击"开始识别"

**识别结果**：
- 生成带时间戳的逐句文本
- 显示每个句子的开始/结束时间
- 提供词级时间戳（在线模式）

---

### 4. AI内容分析（第3步）

**功能说明**：自动分析每句口播文本的角色和重要性

**分析维度**：
1. **角色类型**（role）：
   - `hook` - 开头钩子句（吸引注意力）
   - `selling_point` - 产品卖点句（描述产品优势）
   - `demo` - 展示演示句（展示产品效果）
   - `cta` - 逼单促单句（催促下单、讲价格）
   - `filler` - 过渡/废话（口头禅、无实质内容）

2. **重要程度**（importance）：1-5分
3. **情绪强度**（emotion）：calm/excited/urgent
4. **精简字幕**（subtitle）：去掉口头禅、重复词的精简版

**配置要求**：
- API Key（LLM API密钥，如OpenAI或兼容服务）
- API地址
- 模型名称（如 `gpt-4`、`gpt-3.5-turbo`）

**分析结果展示**：
- 每句话显示角色标签（钩子/卖点/展示/逼单/废话）
- 显示重要程度评分
- 显示情绪强度
- 显示精简后的字幕文本

---

### 5. 生成变体（第4步）

**功能说明**：根据分析结果自动生成多个视频剪辑版本

**变体生成规则**：
- 可配置最短/最长片段时长
- 可配置生成变体数量
- 根据句子角色和重要性智能组合片段
- 自动去除废话片段
- 优化片段顺序和流畅度

**配置参数**：
- 最短片段时长（秒）
- 最长片段时长（秒）
- 变体数量（建议3-10个）

---

### 6. 导出视频（第5步）

**功能说明**：批量导出所有生成的变体版本

**导出设置**：
- 选择输出文件夹
- 是否添加字幕（可选）
- 输出格式：MP4（H.264编码）

**导出参数**：
- 视频编码：libx264
- 编码预设：fast
- 画质参数：CRF 18（高质量）
- 音频编码：AAC 128kbps
- FastStart优化：启用

**导出进度**：
- 显示当前导出进度（第X个/共Y个）
- 完成后显示成功导出的文件列表
- 显示失败的错误信息（如有）

**导出后操作**：
- 点击"打开输出文件夹"直接查看导出文件

---

## 开发命令

### 开发模式

```bash
npm run dev
```

启动开发服务器，自动打开Electron窗口，支持热更新。

### 编译项目

```bash
npm run build
```

编译主进程、预加载脚本和渲染进程，生成 `out/` 目录。

### 打包发布

```bash
npm run dist
```

打包成Windows安装程序（NSIS格式），生成 `dist/` 目录。

### 预览模式

```bash
npm run preview
```

预览编译后的应用（不打包）。

---

## 技术架构

### 前端技术栈

- **React 19** - UI框架
- **Ant Design 6** - UI组件库
- **Zustand 5** - 状态管理
- **Vite 7** - 构建工具

### 后端技术栈

- **Electron 42** - 桌面应用框架
- **electron-vite 5** - Electron构建工具
- **FFmpeg** - 视频处理

### AI集成

- **Whisper API** - 在线语音识别（支持OpenAI及兼容服务）
- **Transformers.js** - 本地AI模型（可选）
- **LLM API** - 内容分析（支持OpenAI及兼容服务）

---

## API配置建议

### 推荐服务提供商

#### Whisper API（语音识别）

1. **OpenAI官方** - `https://api.openai.com`
   - 模型：`whisper-1`
   - 优势：准确率高，稳定

2. **国内代理服务** - 多个兼容服务可用
   - 价格更低
   - 国内网络友好

#### LLM API（内容分析）

1. **OpenAI** - `https://api.openai.com`
   - 模型：`gpt-4`（推荐）或 `gpt-3.5-turbo`

2. **国内兼容服务**
   - 支持OpenAI API格式
   - 国内网络友好

---

## 常见问题

### Q1: 转移项目后无法启动？

**解决方案**：
1. 确认Node.js和FFmpeg已安装
2. 先执行 `npm run build` 编译项目
3. 再执行 `npm run dev` 启动

### Q2: FFmpeg找不到？

**解决方案**：
```bash
winget install ffmpeg
```

### Q3: 依赖损坏或不完整？

**解决方案**：
```bash
# 删除node_modules重新安装
rmdir /s node_modules
npm install
```

### Q4: ASR识别失败？

**在线模式检查**：
- API Key是否正确
- API地址是否正确（注意 `/v1` 后缀）
- 网络连接是否正常

**本地模式检查**：
- 检查本地模型文件是否存在

### Q5: AI分析返回格式异常？

**检查项**：
- LLM API配置是否正确
- 模型是否支持JSON输出
- 输入文本长度是否超限

### Q6: 导出视频失败？

**常见原因**：
- FFmpeg路径问题
- 视频编码不支持
- 输出目录权限问题

---

## 性能优化建议

### 视频处理

- 输入视频建议不超过30分钟
- 建议使用H.264编码的MP4文件
- 分辨率建议1080p或以下

### ASR识别

- 在线模式识别更快、更准确
- 本地模式需要较长加载时间（首次）

### AI分析

- 使用 `gpt-4` 效果更好
- 使用 `gpt-3.5-turbo` 成本更低

### 导出速度

- 导出时间取决于变体数量和视频时长
- 建议每次导出3-5个变体
- 导出期间不要关闭应用

---

## 项目维护

### 更新依赖

```bash
npm update
```

### 检查项目状态

```bash
# 检查Node版本
node --version

# 检查npm版本
npm --version

# 检查FFmpeg
ffmpeg -version
```

### 清理编译缓存

```bash
rmdir /s out
npm run build
```

---

## 联系支持

如有问题或建议，请联系项目开发者。

---

**当前状态**：✅ 项目已完整转移并成功启动，所有功能可用。

**最后更新**：2026-06-29